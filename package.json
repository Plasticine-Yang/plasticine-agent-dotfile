{
  "name": "plasticine-agent-dotfile",
  "version": "0.2.0",
  "type": "module",
  "bin": {
    "plasticine-agent-dotfile": "dist/cli.js",
    "pad": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md"
  ],
  "packageManager": "pnpm@10.8.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "rolldown -c",
    "dev": "rolldown -c -w",
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "format": "biome format . --write",
    "format:check": "biome format .",
    "check": "pnpm lint && pnpm format:check && pnpm typecheck && pnpm test",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "prepack": "pnpm build",
    "prepublishOnly": "pnpm release:check",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "release:check": "pnpm check",
    "release:publish": "tsx ./scripts/publish-release.ts",
    "release:publish:preview": "tsx ./scripts/publish-preview-release.ts",
    "release:preview": "gh workflow run preview-release.yml --ref \"$(git branch --show-current)\" -f ref=\"$(git branch --show-current)\""
  },
  "dependencies": {
    "@clack/prompts": "latest",
    "commander": "latest"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@changesets/cli": "latest",
    "@types/node": "latest",
    "rolldown": "latest",
    "tsx": "latest",
    "typescript": "latest",
    "vitest": "latest"
  }
}
